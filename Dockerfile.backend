# FROM python:3.12-slim

# WORKDIR /app

# # Copy dependency file
# COPY requirements.txt /app/
# RUN pip install --no-cache-dir --upgrade pip \
#     && pip install --no-cache-dir -r requirements.txt

# # Copy app code
# COPY . /app

# # Expose API port
# EXPOSE 8001

# # Start FastAPI app
# CMD ["uvicorn", "backend:app", "--host", "0.0.0.0", "--port", "8001"]


FROM python:3.12-slim
WORKDIR /app

# (optional) small build tools if chromadb/pypdf need them
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential gcc curl git \
 && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir --upgrade pip \
 && pip install --no-cache-dir -r requirements.txt

# copy only backend code
COPY backend.py load_data.py working_mcp_server.py ./
# if you have a 'tools' module you import:
# COPY tools/ ./tools/

EXPOSE 8001
CMD ["uvicorn", "backend:app", "--host", "0.0.0.0", "--port", "8001"]
