# FROM python:3.12-slim

# WORKDIR /app

# COPY requirements.txt .
# RUN pip install --no-cache-dir --upgrade pip \
#     && pip install --no-cache-dir -r requirements.txt

# COPY working_mcp_server.py .
# # Make sure Python can import from /app

# CMD ["fastmcp", "run", "working_mcp_server.py:mcp", "--transport", "http", "--host", "0.0.0.0", "--port", "8000"]


FROM python:3.12-slim
WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir --upgrade pip \
 && pip install --no-cache-dir -r requirements.txt

COPY working_mcp_server.py ./

EXPOSE 8000
# If fastmcp CLI is in requirements.txt:
CMD ["fastmcp", "run", "working_mcp_server.py:mcp", "--transport", "http", "--host", "0.0.0.0", "--port", "8000"]
# OR without the CLI:
# CMD ["python", "working_mcp_server.py"]
